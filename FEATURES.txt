AI MEDIA STUDIO — FEATURES
Real-estate focused AI image, video, and audio workstation
Built with Grok 4.5

Setup, keys, Resolve path, and sharing: see README.md (source of truth for install).
This file lists product capabilities.
What’s new by version: RELEASE_NOTES.md.

HARD RULE: When shipping user-visible changes, update README.md, FEATURES.txt,
and RELEASE_NOTES.md as needed (see AGENTS.md).

==============================================================================
OVERVIEW
==============================================================================
Pay-per-use desktop app for listing videographers. Stage empties, lock camera
moves, invent stills then bridge them, repair stills/video, denoise clips,
interpolate / slow-mo, generate audio, and run creative shots — then send
results to DaVinci Resolve.

Providers:
  fal.ai   — main generation (Studio, Tools, Vision, Audio; FE 1080p proxy).
             Admin key needed only to display fal balance.
  xAI      — Grok Enhance / QC (optional). Grok Imagine on fal uses FAL key.
  Runware  — Frame Editor / Aleph only (optional).

First run: Quick Start wizard. Reopen via Help (?) anytime.

==============================================================================
TOP BAR
==============================================================================
- Settings (gear) — keys, output folder, retention, clear caches, cost guard
- Help (?) — Quick Start, open FEATURES.txt / README.md
- fal balance (Admin key)
- Runware balance / status (Aleph)
- xAI billing
- Import from Resolve
- Output folder path (persisted) + Open folder

==============================================================================
CHARACTERS (reusable stills)
==============================================================================
Peer tab to save realtor / talent / hero stills for one-click reuse — local only
(data/characters.json + data/character_stills/). Not a Library replacement;
Voice Clone stays under Audio.

- New character (primary): upload into any slot (Front/Side/Close-up; one still enough)
  → name → Save; or T2I builder (helpers + Enhance + sequential Front→Side→Close-up
  with confirm each; full-body Front/Side; pure black clean plate)
- Upload still, or pick from Previously used / From Resolve; optional Open Library
- Name (required) + optional notes / tags → Save
- Identity pack (up to 3): Front (primary) · Side (profile) · Close-up (face);
  list thumb = Front (or first available); Edit manages slots; click thumb → large preview
- Generate missing Front/Side/Close-up from existing identity stills (multi-ref I2I;
  pure black clean plate by default; preview + Confirm; base + costume variants)
- Remove background (Bria RMBG, manual): per slot + all-angles batch; cost shown;
  preview Confirm/Dismiss (no auto-overwrite on upload)
- Costume swap: multi-ref model + resolution picker; cost × slots × resolution;
  per-slot errors/retry; black clean plate; named costume child under parent
- Lock / protect: skips retention auto-delete; lock icon on list; unlock anytime
- Generate variation (optional); Use in Motion Sync on base + costumes
- Shortcuts: Save as character from Motion Sync, Director, Studio Image
- **Character picker** (app-wide): one-click fill of person still slots from
  saved characters / costume variants (Front preferred; “Alice / Red Dress”
  labels). Motion Sync · Director shot ref · Creative Vision start/source ·
  Studio Image I2I/R2I. Clear character unlinks picker only; Upload /
  Previously used / From Resolve / Library paths remain.
- Survives app restart (local store); Voice Clone stays under Audio

==============================================================================
SCENES (reusable location stills)
==============================================================================
Peer tab for location / establishing plates — Character = who; Scene = where.
Local only (data/scenes.json + data/scene_stills/). Not a Library replacement.

- **New scene** primary button (top): clears form, focuses Name + still/generate
- New scene: upload still, or Generate (T2I) from a location description
  (optional Enhance + model); separate Aspect (16:9 Horizontal · 9:16 Vertical ·
  1:1 · 4:3 / 3:4) and Quality (1K/2K or Standard/HD — not mixed into one blob);
  establishing bias + framing language; cost tracks model + quality
- **Plate helpers** (each None/skip): Setting · Type · Time of day · Weather ·
  Activity + optional notes — rebuild location description on change (Studio/music
  builder pattern); Enhance still full-rewrites
- Name (required) + optional notes → Save; aspect stored and shown as badge
- List: **Name** primary (user title, never long T2I prompt), notes secondary,
  aspect badge on thumb; missing/broken still → placeholder (path repair from
  scene_stills/); Edit · Delete · Lock · Show in folder; click thumb to enlarge
  (form / generate result / list)
- Location description box is tall (~2×) so Enhance text is readable
- **Create variation** (I2I): parent still as ref; name + transform prompt + model +
  quality + Enhance + cost; Generate → preview → Confirm / Regenerate / Dismiss;
  saves child under parent’s Variations; clear error if parent still missing
- **Angle pack** (per scene/variation): Hero / wide (required) + Left(B) / Right(C).
  One-click Generate Left / Right / Reverse (I2I from hero; strong lateral camera
  language; does not overwrite hero). Est. cost + model/quality; preview enlarge.
- Scenes support **variations + multi-angle packs** for Director location refs
- Director multi-ref: **Scene pack Hero only | Full pack** (default Hero only when
  character also bound); single-ref always Hero; ref budget unique-asset based
- Lock protects from retention auto-delete (same idea as Characters)
- Empty state points to **New scene**
- Local only: data/scenes.json + scene_stills/ (gitignored like Characters)
- Survives app restart; Director multi-ref scene slots

==============================================================================
MOTION SYNC (character + driving video)
==============================================================================
Peer tab for motion transfer: a character still performs actions from a driving
clip (realtor hooks, trend content) while preserving identity.

- Character still: **Saved character** picker + upload / Previously used /
  From Resolve (full-body or clear upper body, good light, simple background preferred)
- Motion reference: upload video / From Resolve; duration shown when loaded
  (clean single subject, 3–10s ideal; long/large clips auto-proxy — original kept)
- Best-practice tips always visible on the tab
- Models (true motion-control only):
    Kling Motion Control V3 Pro · V3 Standard · Kling 2.6 Motion Control · Wan Motion
- Kling: keep original audio (default on); character orientation match video / image
- Wan: adapt motion · enhance identity · acceleration
- Optional prompt + light helper chips + Enhance
- Est. cost under Generate (Studio chrome; duration × $/s)
- Generate → Library / Show in folder / Send to Resolve
- Clear blocks if still or motion clip is missing; friendly errors for no-person /
  too short / too large / unsupported format

==============================================================================
VFX (in-scene + element plates)
==============================================================================
Peer tab for visual effects — not for general plate staging (use Studio / Vision).

Sub-tabs:
  In-scene — effect integrated into a full still or clip (geometry + lighting lock)
  Element plates — isolated effect on pure black / clean bg for Resolve Screen or Add

Effect preset packs (physics-aware prompt inject; prompt stays fully editable):
  Fire / heat haze · Smoke / dust · Energy / power surge · Rain / snow / fog ·
  Debris / impact · Lens flare / light leak · **Custom** (user-written only; no
  pack inject; Enhance rewrites for model + In-scene/Element without forcing a category)

Shared controls:
  Source still or clip · Strength / intensity · Duration · Resolution ·
  Model picker (Grok Imagine 1.5, Kling, Seedance, H3, Veo Fast, video-edit models) ·
  Enhance (mode-aware) · Est. cost under Generate (Studio chrome) ·
  Library / Show in folder / Send to Resolve

Element plates: obvious “Pure black / clean plate (Screen · Add in Resolve)” checkbox
and tip copy. In-scene keeps plate geometry/lighting language in the prompt.

==============================================================================
DIRECTOR (multi-shot)
==============================================================================
Peer tab to Studio / Creative Vision / Frame Editor — generate multi-shot clips
from a master brief + ordered shot list (not for editing existing plates).

- Master: global prompt, total duration (model max, e.g. 3–15s), aspect, style pack
  (None | Cinematic | Marvel-style | Documentary | Luxury RE), multi-shot model only
- Aspect: only ratios the selected model/path accepts (Kling T2V: 16:9 · 9:16 · 1:1).
  With a shot ref still, Kling I2V multi-shot uses Auto (from start still) — API has no
  aspect_ratio (frame follows the still). O3 I2V field is image_url; V3 uses start_image_url.
  Errors show sent vs accepted when aspect is rejected.
- multi_prompt ≤512 chars/shot (Kling): auto-compact master/continuity; live counts;
  Generate blocked if still over after compact.
- Per shot: **Character = who**, **Scene = where**, **Action = what happens**.
  Character + Scene pickers on every shot card (saved Characters / Scenes + variations).
  Multi-ref models (Kling V3, Imagine 1.5): both stills as real image refs.
  Single-ref (Kling O3): Scene disabled as image; location stays in prompt text
  (“describe location in text” note only on single-ref models).
  Same character / Same location → Apply to all shots. Manual Ref still remains.
  Soft aspect mismatch warn when scene plate ≠ Director aspect.
- **Unique ref budget** (whole job, not per-shot duplicates): Refs N / max (~7) +
  Shots N / max — blue under, amber ≥80%, red over. Over budget disables Generate
  with a short reason. Kling element-style: 1 per unique character (Front+Side+
  Close-up pack = 1) + unique scene plates when multi-ref. Imagine bag-of-images:
  Front only | Full pack toggle (default Front only if any scene bound, else Full
  pack); each selected still counts once if reused across shots.
  **Single-ref models** (FLUX 3 I2V / First→Last, O3): Front only forced — Full pack
  hidden; Side/Close-up never counted or sent. Full pack only when the model accepts
  multi-image identity or scene angle packs. Budget matches what Generate submits.
- **Location (text)** when model is single-ref (e.g. Kling O3): per-shot field,
  auto-filled from Scene name + notes; Action stays character-only. Hidden when
  scene binds as an image ref (multi-ref models).
- **Auto-balance shot times**: evenly split total duration (contiguous ranges);
  manual start/end editable after; Add shot re-balances; live red warn on overlap /
  out-of-range; title shows “Shot N · Xs”.
- **Still ref size**: before Kling/Grok image-ref upload, oversized character/scene
  stills auto-downscale (longest edge ≤1920, ≤~8 MB JPEG proxy); originals kept.
  Status notes “Using downscaled refs for API”. Image “too large” errors never
  suggest Render-in-Place video proxy.
- Models (multi-shot / reference): Kling V3 Pro / Standard Multi-Shot, Kling O3 Pro /
  Standard Multi-Shot; **Grok Imagine 1.5 · Reference storyboard** (single clip from
  ordered shot refs via T2V/I2V/R2V — not Kling multi_prompt); **FLUX 3 · Continuous I2V**
  and **FLUX 3 · First→Last** (single continuous take / start→end — not multi_prompt cuts).
  Seedance / Wan / H3 stay single-clip outside Director.
- FLUX 3 Director: up to 20s · 720p/1080p · generate_audio; cost ~$0.17/s @720p ·
  ~$0.29/s @1080p; Continuous = character/start still; First→Last = Shot 1 + Shot 2 stills
- How-to: FLUX 3 = continuous take / first→last; Kling = multi-shot cuts
- Est. cost: Studio-style bordered block directly under Generate; live on model /
  duration / audio / resolution (when exposed)
- Shot list 2–6 rows (hard-capped by model): start/end time, camera preset
  (Push in / Orbit / Crane / Static / Pan / Tilt / Handheld…), per-shot action,
  Character + Scene pickers + optional manual Ref still (Previously used /
  From Resolve) with thumbnail (Clear; empty “No character / scene still”)
- Phase 2 polish (prompt-level): audio intent (Generate audio + No music | Soft bed |
  Full score + optional SFX notes); continuity toggles (Same character / location /
  time of day); per-gap transition (Hard cut | Soft dissolve | Continuous — no cut,
  refs as motion keyframes) with global default applied to all gaps + control between
  Shot N and N+1; Continuous fail-safe needs ordered times + ref stills on both sides;
  optional energy curve (restrained → peak → resolve); vision notes (Enhance-only);
  output Single multi-shot clip (default) or Clip pack + shot-list .txt sidecar
- Fail-safes: times inside [0, duration], no overlaps, min 1s per shot, required
  action prompts; Generate blocked with clear messages
- Enhance: master + style + polish + every shot → shot-aware rewrite (vision notes
  weave into tone; not dumped raw unless useful)
- Generate → one multi-shot video (fal multi_prompt); optional shot-list sidecar for
  Resolve; Library + Show in folder + Send to Resolve
- Library Send to ▾ → Director ▶ → Shot 1…N assigns that shot’s ref still + thumbnail
  (creates Shot 1 if empty; focuses Director + highlights the row)
- Cost est. = duration × model $/s

==============================================================================
JOB / LISTING (app-level)
==============================================================================
Optional “Job / Listing” field on the scenario bar (shared by Studio, Creative
Vision, Tools, Frame Editor, Audio):
  - e.g. address, client, shoot date
  - Last-used value is persisted across sessions
  - When set, final media is saved under:
        <output_root>/jobs/<safe-job-name>/<YYYY-MM-DD>/
  - Empty = current flat dated layout:
        <output_root>/<YYYY-MM-DD>/
  - History / Library metadata store the job label; no cloud sync
  - Caches (_region, previews, Aleph proxies) stay under the output root
  - Library can also assign (or clear) a job after the fact — see LIBRARY

==============================================================================
SCENARIOS (app-level)
==============================================================================
Shared scenario bar configures applicable workspaces:
  Blank / Custom
  Furniture Pop-in
  Furniture Swap
  Day → Night
  Twilight Exterior
  Sky + Mood
  Lot to Home
  Dehaze / Clear Air
  Landscaper
  Amenity On
  Season Change

Blank / Custom = freeform, no forced builder.
Scenario prompts are defaults and stay editable.

==============================================================================
STUDIO — IMAGE
==============================================================================
Same-row modality sub-tabs (to the right of Image | Video):
  I2I | T2I | R2I | Region  (default I2I; model list filtered by sub-tab)
- I2I — single source still; image-edit models only
- T2I — no source required; text-to-image models; Size/aspect from T2I presets
- R2I — primary + multi-ref stills; only models with max_refs > 1
- Region — colored boxes + per-box prompts (Seedream annotation); was Standard/Region toggle
- Source still: **Saved character** picker + upload + Previously used + From Resolve
- Multi-reference stills (R2I only):
    Primary source + optional “Add reference” slots up to model max
    Nano Banana family / Seedream / Flux multi-ref / Grok Imagine: multi-ref
    Single-image models (e.g. Flux Kontext, MAI): not listed under R2I
    Region mode: annotated primary only (no extra refs)
- Leaving Region clears box overlays so they do not appear on I2I
- Model list (Flux 2 Pro/Max Edit, MAI-Image, Seedream, etc.)
- # Images 1–4 multi-variant (when model multi-outputs in one call, or sequential
  singles if the API is one-at-a-time — e.g. Flux 2 Pro max 1 per call).
  Est. cost always = per-image rate × selected count (never capped at 1× for
  sequential batch). Label e.g. $0.12 · 4 images · 4 sequential runs (Model).
  Each still is a separate Library item (Send to ▾ per variant). Per-tab busy only.
- Strength / relevant controls
- Grok Enhance (vision-aware, model-specific, scenario-aware; can attach/mention
  extra refs when multi-ref is set)
- Comparison viewer with Overlay / A/B + opacity
- Versions history
- Cost estimate (job total), generate, Show in folder, Send to Resolve, Send to ▾
- Export before/after ▾ when source still + result exist:
    Side-by-side (labeled) or vertical stack (phone posts)
    Saves under job/dated output folder; reveals in Explorer/Finder

Region edit:
  - Draw/place colored boxes on the still (small left + large Comparison stage)
  - L/T/W/H are % of the source image content box (CONTAIN letterbox); both
    previews share that coordinate system (no panel-size offset between them)
  - Large stage: full-brightness source while placing boxes; region boxes live
    on the image content rect only (not a full-stage host); gen blend omitted
    from the stack in source-only mode (no grey veil)
  - Per-box prompts; composite hard-fails if annotation still cannot be built
    (never ships a raw still + box text as the model input)
  - Model list filtered to Seedream / annotation-capable models

==============================================================================
STUDIO — VIDEO
==============================================================================
Same-row modality sub-tabs (to the right of Image | Video):
  I2V | T2V | V2V | R2V  (default I2V)
  Model dropdown filtered strictly by sub-tab; MiniMax H3 Omni under R2V
  T2V cost = rate × duration (Vision T2V / H3 T2V; not image-edit fallback)
  R2V Studio = one still + optional motion clip as Video 1; full multi-image
    + audio omni stays Creative Vision · Omni reference
Secondary workspaces: Received, Blank Canvas, Camera Lock (map → I2V / V2V)
- Source video + reference still (from Image “use as reference”)
- Recently from Resolve (clips / stills from handoff history)
- Camera-lock style prompts for furniture / day-night / etc.
- Models: Kling family, Seedance 2.0 I2V/V2V/reference, MiniMax H3 I2V + Omni
  Reference (still + optional motion plate as Video 1), **Grok Imagine Video 1.5**
  I2V + R2V (up to 7 stills), **FLUX 3** I2V + First→Last + Extend (V2V), others on fal
- MiniMax H3: 5–15s · 2K · ~$0.26/s; native stereo on output
- Grok Imagine 1.5: 1–15s · 480p–1080p; native audio; ~$0.08–0.25/s + $0.01/ref
- FLUX 3 (BFL on fal): 5–20s (or auto) · 720p/1080p · optional native audio ·
  ~$0.17/s @720p · ~$0.29/s @1080p; First→Last requires start + end stills
- Grok Enhance on video prompts (modality-aware citations for R2V / first–last I2V)
- Duration / resolution guidance, cost estimate (job total)
- Export: Show in folder, Send to Resolve, Library

==============================================================================
TOOLS
==============================================================================
Split: Image tools | Video tools
Large result viewer with Overlay / A/B where applicable (CONTAIN previews)
Previously used + From Resolve strips on tools that accept media
  (image tools: handoff stills; video tools: handoff clips)
Per-tab busy scopes so long jobs do not freeze every tab

Image tools:
  Upscale, Object Remove, Sky / Weather, Dehaze, Sharpen / Restore
    (CodeFormer face fidelity, NAFNet whole-frame deblur, prompt restore;
     with ref still: multi-image identity-lock defaults — Nano Banana / Flux / …;
     Soft source + Reference still stacked vertically so labels/upload are not clipped),
  Inpaint (freehand) — still only, 3-column layout (controls | large canvas |
    result): brush/eraser/undo/clear/size on center; canvas zoom (± / Fit /
    scroll wheel) + Pan mode for fine edges (brush is constant screen-px);
    strokes map to full-res mask (EXIF-normalized source WxH); Grow mask
    0/2/4/8 px (default 4) dilates white region at export only for edge blend;
    # Images 1–4 when endpoint supports num_images (cost × N); optional or
    required Ref still when the model accepts reference (Kontext requires;
    LoRA Fill optional; Pro Fill / Dev / Juggernaut hide ref UI);
    intent helpers (change/add/remove object, sky/background, reflection,
    custom); prompt + optional negative; mask-capable models only
    (Flux Pro Fill, Flux LoRA Fill, Flux Dev Inpaint, Flux Kontext LoRA
    Inpaint, Juggernaut Flux LoRA Inpaint — not Nano Banana / Flux 2 edit);
    empty mask blocks Run; multi-image results land in Library as separate
    stills; status logs image/mask WxH on failure; result pane on the right,
  Blown Out, Mirror / Glass, Amenity On, Season / Curb, Match Look, Re-Aspect

Model dropdowns (Studio, Vision, Tools, …):
  Under-dropdown “Best for: …” line (~6–12 words) + tooltip detail when a
  registry entry exists; unknown models hide the line (no empty Best for).

Video tools:
  Upscale (SeedVR2, Bytedance, Topaz families: Proteus / Artemis / Nyx /
           Starlight / Gaia, RealESRGAN)
  Denoise / Clean (Topaz Nyx + Artemis; noise / compression / detail / halo)
  Slow Mo / Interpolate (RIFE default, FILM for large motion; 2×–5× factor)
  Object remove, Sky / Weather, Mirror / Glass, Amenity On, Restore, Re-Aspect

Each tool: model select, prompt + Enhance where prompts exist (not pure Topaz
control jobs), cost estimate as job total, result → play / Show in folder /
Resolve / Library. Image tools with source: Export before/after ▾
(side-by-side or vertical stack) on the large result pane.

==============================================================================
CREATIVE VISION
==============================================================================
Cinematic playground (separate from listing camera-lock staging).
Modes (left → right):

  Text → Image (T2I)
    Invent a still with no source required. Still-only helpers — no camera
    motion language (no push-in / pan / orbit on rebuild or Enhance).
    Models (fal, as registered): Flux 2 Pro / Flux 2 / Flex / 1.1 Pro Ultra,
      Nano Banana / Nano Banana 2 / Pro, Seedream 4.5 / 5.0 Lite / 5.0 Pro,
      Recraft V3
    Still photography helpers:
      Framing / composition · Lens look · Lighting · Still style presets
      Each helper includes (None) = omit from Rebuild + Enhance
    Multi-variant: # Images 1–4 (UI batch; sequential when API is 1-at-a-time)
      Cost estimate = per-image × selected count (full job total, not capped at
      API max). Label notes “N sequential runs” when batching singles.
      Multi-output in one fal call when the model allows; else sequential
      singles (vision tab busy only — not a global queue)
      Each still lands as a separate Library item with its own Send to ▾
      Variant strip in Result to select which still to send
    Result Send to ▾ includes → Start frame / → End frame (this tab) for
    bridge handoff, plus Studio / Frame Editor / Tools / Resolve as elsewhere

  Image → Image (I2I) — creative still edit (Aleph plate / multi-ref)
    Primary source still required (upload, Previously used, From Resolve, or
    Send → Creative Vision · I2I source).
    Multi-ref: up to 3 extra reference stills (identity, material, furniture,
    sky…) when the edit model accepts multiple image inputs (Flux 2 family,
    Nano Banana family, Seedream). Chips + Add ref / Clear; strip target
    Source vs Add as ref. Single-image models (e.g. Flux Kontext Pro): extra
    refs hidden/disabled with a short note; generate uses primary only.
    Models (fal edit): Flux 2 Pro / Max / Flex, Flux Kontext Pro,
                       Nano Banana / 2 / Pro edit, Seedream 5 Pro edit
    Strength slider when the API exposes it (Flux family)
    Same still-only helpers + (None); Creative direction for Enhance only;
    Grok Enhance with vision on primary (+ refs when multi-ref) and prompt
    language naming ref roles (no invented API fields)
    Result → Frame Editor · keyframe, Start / End / I2V, Library, Resolve
    Send matrix: I2I source + Add as I2I ref

  Text → Video (T2V)
    e.g. Veo 3.1 / Veo 3.1 Fast / Reference pack, Luma Ray 2,
         MiniMax H3 T2V, MiniMax H3 Omni reference, **Grok Imagine 1.5 T2V / Reference pack**,
         **FLUX 3 T2V**
    Veo on fal: ~$0.40/s standard, ~$0.15/s Fast (totals = rate × duration)
    MiniMax H3 (Hailuo-03) on fal (minimax/h3/*):
      - T2V: prompt · duration 5–15s · aspect · 2K · native stereo
      - Omni reference (primary when refs exist): up to 9 images + 3 videos +
        3 audio (≤12 files); cite Image 1 / Video 1 / Audio 1 in prompt
      - Est. ~$0.26/s @2K (+ ref image/video surcharges per fal docs)
      - Native stereo always on output (Send to Resolve / Library as video+audio)
    Grok Imagine Video 1.5 on fal (xai/grok-imagine-video/v1.5/*):
      - T2V / I2V / R2V (1–7 stills; tag <IMAGE_0>…); native audio; 1–15s
      - Est. $0.08/s @480p · $0.14/s @720p · $0.25/s @1080p + $0.01/ref image
      - Also Studio Video I2V/R2V + Director reference storyboard
    FLUX 3 Video on fal (blackforestlabs/flux-3/*):
      - T2V / I2V / first→last / extend · 5–20s (or auto) · 720p/1080p
      - Optional generate_audio · Est. ~$0.17/s @720p · ~$0.29/s @1080p
      - Also Studio Video I2V / First→Last / Extend (V2V)

  Image → Video (I2V)
    Start still → motion (Veo, Kling O3 Pro, Seedance 2.0, Hailuo 02, MiniMax H3,
      Grok Imagine 1.5, FLUX 3, …)
    MiniMax H3 I2V: first frame still; optional last-frame still (first→last
      transitions: day→night, porch→interior); 5–15s · 2K · native stereo
    Grok Imagine 1.5 I2V: start still + motion prompt; native audio; 480p–1080p
    FLUX 3 I2V: start still + motion; optional audio; 5–20s · 720p/1080p

  Bridge / Connect
    Start frame + end frame → in-between (Veo first→last, Hailuo start+end,
      **FLUX 3 first→last**)
    (H3 first→last also available as I2V with optional end frame)

  Extend Video
    Source clip + prompt → continued motion (**FLUX 3 extend-video**)
    Source under 50 MB / 15s on fal; duration / res / audio as above

Shared Vision UI:
  - Video modes: Camera / shot helpers (shot type, lens feel, motion, style)
  - Still modes (T2I / I2I): still photography helpers only (no motion)
  - Every helper dropdown includes (None) — omit that dimension from Rebuild
    prompt and from Enhance guidance (active helpers only)
  - Rebuild prompt: Replace / Append from active helpers only
  - Creative direction for Enhance (optional field):
      Enhance-only intent for Grok — never sent raw on Generate
      Empty = current behavior (helpers + prompt only)
  - Subject Library + Vision presets (collapsible expanders)
  - Grok Enhance (model-aware; still modes never add push-in / pan language)
  - Negative prompt (optional)
  - Cost labels are job totals, e.g.
      Est. cost: $3.20 · 8s (Veo 3.1)
      Est. cost: $0.03 · 1 image (Flux 2 Pro (T2I))
      Est. cost: $0.12 · 4 images (Flux 2 Pro (T2I))
    never a bare unit rate alone
  - Optional cost guard (Settings: warn at $2+ / $5+; default off)
  - Result: play (video, CONTAIN) / preview (still), Library, Resolve, Send to ▾
  - **Saved character** picker + Previously used + From Resolve on I2I / I2V /
    bridge start (person stills)

Helper (None) also applies to Studio scene-builder helpers (style / density /
camera) and Audio prompt builders (Music / SFX / Ambience / Video→SFX chips)
where a dimension injects text into Rebuild — Off/None skips that text.

==============================================================================
FRAME EDITOR (Aleph 2.0)
==============================================================================
Requires Runware API key — not fal (fal only for optional 1080p proxy).

Keyframe-guided video edit (Aleph frameImages: first | last | timestamp + prompt):
  - Upload source video; Previously used + From Resolve (stills → pin, clips → source)
  - Auto-downscale to 1080p-class proxy when needed (fal scale-video); 2–30s source
  - Edit intent helper (UI only — no invented API params):
      Apply through clip | Transition first→last | Custom timestamps
      One keyframe ≈ apply look through clip; first+last ≈ transition between looks
      Day→night tip: pin day @ first, night @ last, then Generate
  - Dense filmstrip (more thumbs on short clips; more + horizontal scroll on long)
      Click = preview; Pin frame = keyframe slot; selected / pinned times highlighted
  - Up to 5 keyframes with clear position badges (first / last / 1.61s) + image name
  - Send frame to Studio for cleanup gens → pin still back
  - Prompt + Grok Enhance (transition intent steers dual-anchor language;
      motion / architecture locked; change-only wording)
  - Sticky Generate + est. cost on left rail
  - Generate via Aleph 2.0
  - Result: full in-app play (CONTAIN, no crop) with play/pause controls;
      Show in folder / Send to Resolve / Send to ▾ under player (reachable)
  - Result → Library, Show in folder, Send to Resolve

==============================================================================
AUDIO
==============================================================================
- Music generator (structured builder) + arrangement (intro/lift/solo/outro + band layers);
    target duration in prompt (hard limit language) + post-generate Trim to Ns (ffmpeg)
- SFX (categories + prompt builder)
- Layered SFX mixer (v1) — plain Bed / Spot / Accent labels + subtitles;
    Scene Enhance: one scene description → fills all 3 layer prompts (Grok;
    no auto-generate); Generate all = unmuted layers with prompts (sequential);
    per-slot Generate / Enhance + intent helpers; Bed/Spot: Load from Video → SFX;
    mute + volume; Bounce mix + optional stems; cost sums layers that will run;
    MP3 → WAV sidecar for mix (pygame; no system ffmpeg)
- Ambience (scene builder)
- Voiceover (tone/emotion; optional character/clone paths)
- Video → SFX (synced Foley from clip)
    Models: MMAudio V2, Mirelo, Sonilo, Kling video-to-audio (as registered)
    Char counter + warning near Kling ~200-char limit (no silent-only truncate)
    Style / Pace / Emphasis / Exclude chips + Enhance
- In-app playback where supported
- Show in folder, Send to Resolve
- Library Audio filter
- Cost estimates as job totals (same format_job_cost pattern as Vision)

==============================================================================
LIBRARY
==============================================================================
- Filters: All | Image | Video | Audio | From Resolve
- From Resolve: handoff-origin stills/clips (plugin Send or Import from Resolve)
- Resolve badge on cards in All (and other filters) when origin=resolve
- History stores origin=resolve on import so filter/badge stay reliable
- Job / Listing dropdown: All jobs or a specific label from history
- Visual group headers by job when viewing All jobs
- Job label shown on cards when set
- Assign to ▾ on every card (Image / Video / Audio) — same weight as Send to:
    New Job / Listing… (prompt → create + assign)
    Existing job names from history + jobs/ folders
    Clear job (Ungrouped)
    Always updates history metadata; safely moves files under
    outputs/jobs/<slug>/<date>/ when they already live under the output root
    (outside-root paths stay put — metadata only). Clear does not reverse-move.
    Grouping / filters refresh immediately after assign
- Bulk select: checkbox on each card → bulk bar (N selected · Assign to ▾ ·
  Send to Resolve · Select all · Clear).
    Assign: same New / existing / Clear menu as single-card; metadata + optional
    file move for every selected item.
    Send to Resolve: Tier A per item (bin AI Media Studio / job or date; optional
    V2/playhead + marker); images + videos mixed OK; continues on per-item failure
    except Resolve unavailable / no project (stops batch with one message);
    snack summary “N sent, M skipped (…)”. Single-card Send to menu unchanged.
- Local spend panel (from history costs only):
    Today / This week / This month / All time totals
    Top models + by provider; missing or $0 cost rows skipped
    Also summarized in Settings → Local spend
- Hide missing media (Settings)
- Thumbnails + metadata (model, job, scenario, cost, time, Resolve badge)
- Send to ▾ nested: Studio Image / Video · Region · Director ▶ (Shot 1…N) ·
  Creative Vision ▶ · Frame Editor · Tools ▶ · Resolve (not a long flat list)
- Show in folder (uses updated path after assign-move)
- Open large (stills / posters)
- History cap; media retention follows Settings policy

==============================================================================
RESOLVE INTEGRATION
==============================================================================
- Import from Resolve (stills / clips via handoff folder) + auto-watch handoff
- Send to Resolve (Tier A smarter send) from Studio, Tools, Frame Editor, Audio, Library
    (DaVinci Resolve logo asset on Send buttons):
    - Media Pool bin: AI Media Studio / <Job name or today’s date>
    - Optional place on video track (default V2) at playhead when scripting allows
    - Optional timeline/clip marker note (model · scenario · cost when known)
    - Soft fail: Resolve closed / scripting off → reveal file folder; never crash
    - Does not render-in-place, build compounds, auto transitions, or bake grade
- From Resolve strips wherever media can load:
    Studio Image / Video, Tools image + video forms, Creative Vision
    (I2I / start frames), Frame Editor (stills → pin, clips → source)
- Handoff import records Library rows with origin=resolve
- Studio path auto-detected (no hardcoded personal path)
- Prefer graded proxies; Render in Place when needed (manual in Resolve)
- Handoff auto-purge (~7 days / ~200 files) + Clear handoff cache
- Single-instance: Resolve Send reuses the open Studio window (no second UI)

==============================================================================
SETTINGS / DISK SAFETY
==============================================================================
- fal / xAI / Runware keys (local app data only)
- Output folder (persisted across sessions)
- Library / outputs retention: Never / 7 / 14 / 30 / 90 days
- Apply retention now
- Clear caches (app-owned dirs only under output root + handoff —
  never deletes arbitrary user folders outside the app tree)
- Cost guard for expensive generates (default off; warn at $2+ or $5+)
- Hide missing Library rows
- Check for updates on startup (default on):
    Compares APP_VERSION + git SHA with GitHub (release tag, then commit SHA;
    same-day date alone does not flag behind)
    Quiet banner + snack with Open GitHub link — no auto-download
    Fails soft when offline; toggle in Settings → Storage
    Help → Check for updates… anytime
- Single-instance app lock (app data): second launch (e.g. Resolve Send)
    signals the open window and exits — no second UI. Fail soft if lock
    unavailable.

==============================================================================
COST LABELS
==============================================================================
UI cost lines are estimated job totals, not bare rates:
  Est. cost: $X.XX · {unit} ({model})
  e.g. $3.20 · 8s for Veo 3.1 standard ($0.40/s × 8s on fal)
  e.g. $0.90 · 6s for Veo 3.1 Fast ($0.15/s × 6s)
  e.g. $0.03 · 1 image for Flux T2I / Studio Image edit
  e.g. $0.12 · 4 images · 4 sequential runs when API is 1-image-at-a-time
Still batch: always rate × selected # Images (Studio Image, Vision T2I, …).
Estimates are conservative UI ballparks, not billing. No invented audio
surcharge unless the provider quotes one.

==============================================================================
PER-TAB BUSY
==============================================================================
Long-running jobs lock only their scope (image, video, tools, vision,
audio, frame_editor, enhance, …). Other tabs stay usable.

==============================================================================
PROMPT FAVORITES + PACKS
==============================================================================
Light favorites (no marketplace):
  - ★ Star current prompt (user or post-Enhance text) from Studio Image,
    Creative Vision, Tools (prompt tools), Frame Editor, Audio panels
  - Favorites dropdown + Apply reloads into the active prompt box
  - Stored under app data (prompt_favorites.json) — not secrets, not outputs/
  - Export / Import a small JSON “prompt pack” (name + prompts + optional
    scenario tags). Studio Image and Music include Export/Import; other
    surfaces share the same favorites store via Star/Apply

==============================================================================
GROK ENHANCE
==============================================================================
Available across Studio, Tools, Creative Vision, Frame Editor, Audio:
  - Rewrites prompts for the selected model
  - Uses vision when a still/reference is present
  - Scenario-aware constraints
  - Does not auto-switch models
  - Creative Vision: optional “Creative direction for Enhance” field is
    Enhance-only intent (empty = helpers + main prompt only; never raw on Generate)
  - Helper (None) values are omitted from Enhance guidance
  - T2I / I2I Enhance never injects camera motion language
  - Skipped where tools are pure control (e.g. Topaz denoise)
  - Star the enhanced text after Enhance to keep it in Favorites

==============================================================================
PACKAGING
==============================================================================
- MIT License
- README: Windows / macOS / Linux install
- Launchers: start.bat, start.command, start.sh (skip pip when satisfied)
- .env.example placeholders only; no personal keys or paths in repo
- .gitignore: outputs/, handoff, .env, venv, caches
- Quiet credit: Built with Grok 4.5

==============================================================================
DESIGN PRINCIPLES
==============================================================================
- Real-estate defaults: preserve architecture, camera, identity unless asked
- Pay only for what you generate
- Studio = listing workflow; Creative Vision = invention (still → bridge →
  shot); Frame Editor = fix-a-frame-through-motion
- Resolve is the timeline; this app is the AI workbench
